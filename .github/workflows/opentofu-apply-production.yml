name: 'OpenTofu Apply - Production'

on:
  push:
    branches: [main]
    paths:
      - 'opentofu/**'
      - 'V2V2B/**'
      - '.github/workflows/opentofu-apply-production.yml'

permissions:
  contents: write
  id-token: write
  issues: write

env:
  OPENTOFU_VERSION: '1.10.0'
  WORKING_DIR: './opentofu'
  ENVIRONMENT: 'production'
  # Azure Environment Variables
  ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
  ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
  ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
  ARM_USE_OIDC: true

jobs:
  apply-production:
    name: 'Apply to Production'
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup OpenTofu
        uses: opentofu/setup-opentofu@v1
        with:
          tofu_version: ${{ env.OPENTOFU_VERSION }}
          tofu_wrapper: false

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: OpenTofu Init
        working-directory: ${{ env.WORKING_DIR }}
        run: |
          tofu init \
            -backend-config="resource_group_name=${{ vars.TF_STATE_RESOURCE_GROUP_NAME }}" \
            -backend-config="storage_account_name=${{ vars.TF_STATE_STORAGE_ACCOUNT_NAME }}" \
            -backend-config="container_name=tfstate" \
            -backend-config="key=${{ env.ENVIRONMENT }}.terraform.tfstate"

      - name: Select Production Workspace
        working-directory: ${{ env.WORKING_DIR }}
        run: |
          tofu workspace select ${{ env.ENVIRONMENT }} || \
          tofu workspace new ${{ env.ENVIRONMENT }}

      - name: OpenTofu Plan
        working-directory: ${{ env.WORKING_DIR }}
        env:
          TF_VAR_azure_subscription_id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          TF_VAR_telegram_bot_token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TF_VAR_github_tokens: '{"maestro":"${{ secrets.GH_TOKEN_MAESTRO }}","beyond":"${{ secrets.GH_TOKEN_BEYOND }}"}'
          TF_VAR_google_drive_folder_id: ${{ secrets.GOOGLE_DRIVE_FOLDER_ID || '' }}
          TF_VAR_obsidian_drive_folder_id: ${{ vars.OBSIDIAN_DRIVE_FOLDER_ID || '' }}
          TF_VAR_kanban_folder_id: ${{ vars.KANBAN_FOLDER_ID || '' }}
          TF_VAR_drive_poll_interval: ${{ vars.DRIVE_POLL_INTERVAL || '300' }}
          TF_VAR_repos_config_file: 'repos.json'
          TF_VAR_environment: ${{ env.ENVIRONMENT }}
        run: |
          tofu plan \
            -var-file="environments/${{ env.ENVIRONMENT }}.tfvars" \
            -no-color \
            -out=tfplan

      - name: OpenTofu Apply
        id: apply
        working-directory: ${{ env.WORKING_DIR }}
        env:
          TF_VAR_azure_subscription_id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          TF_VAR_telegram_bot_token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TF_VAR_github_tokens: '{"maestro":"${{ secrets.GH_TOKEN_MAESTRO }}","beyond":"${{ secrets.GH_TOKEN_BEYOND }}"}'
          TF_VAR_google_drive_folder_id: ${{ secrets.GOOGLE_DRIVE_FOLDER_ID || '' }}
          TF_VAR_obsidian_drive_folder_id: ${{ vars.OBSIDIAN_DRIVE_FOLDER_ID || '' }}
          TF_VAR_kanban_folder_id: ${{ vars.KANBAN_FOLDER_ID || '' }}
          TF_VAR_drive_poll_interval: ${{ vars.DRIVE_POLL_INTERVAL || '300' }}
          TF_VAR_repos_config_file: 'repos.json'
          TF_VAR_environment: ${{ env.ENVIRONMENT }}
        run: tofu apply -auto-approve tfplan

      - name: Get OpenTofu Outputs
        if: steps.apply.outcome == 'success'
        id: outputs
        working-directory: ${{ env.WORKING_DIR }}
        run: |
          echo "function_url=$(tofu output -raw function_url 2>/dev/null || echo '')" >> $GITHUB_OUTPUT
          echo "function_name=$(tofu output -raw function_name 2>/dev/null || echo '')" >> $GITHUB_OUTPUT
          
      - name: Update Telegram Webhook
        if: steps.apply.outcome == 'success'
        run: |
          FUNCTION_URL="${{ steps.outputs.outputs.function_url }}"
          # Azure Functions usually have a specific endpoint for the webhook, e.g., /api/telegram_webhook
          # Assuming the Python code handles /api/telegram_webhook or similar. 
          # The output.tf had function_url as the hostname, so we might need to append path
          WEBHOOK_URL="${FUNCTION_URL}/api/telegram_webhook"
          
          if [ -n "$FUNCTION_URL" ]; then
            echo "Updating Telegram webhook to: $WEBHOOK_URL"
            curl -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/setWebhook?url=$WEBHOOK_URL"
            echo ""
            echo "✅ Telegram webhook updated"
          fi
        continue-on-error: true

      - name: 🏷️ Generate DORA Deployment Tag
        if: success()
        run: |
          TAG_NAME="deploy-prod-$(date +'%Y%m%d-%H%M%S')"
          echo "Creating release tag: $TAG_NAME"
          git tag $TAG_NAME
          git push origin $TAG_NAME

      - name: Create Deployment Summary
        if: always()
        run: |
          cat >> $GITHUB_STEP_SUMMARY <<EOF
          ## 🚀 Production Deployment Summary (Azure)
          
          **Environment:** \`${{ env.ENVIRONMENT }}\`
          **Status:** ${{ steps.apply.outcome == 'success' && '✅ Success' || '❌ Failed' }}
          **Function URL:** ${{ steps.outputs.outputs.function_url }}
          EOF

      - name: Notify on Failure
        if: failure()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: ${{ vars.NOTIFICATION_EMAIL }}
          password: ${{ secrets.EMAIL_APP_PASSWORD }}
          subject: '❌ Maestro Production Deployment Failed (Azure)'
          to: ${{ vars.NOTIFICATION_EMAIL }}
          from: 'Maestro GitOps <${{ vars.NOTIFICATION_EMAIL }}>'
          body: |
             Production deployment to Azure failed.
             Commit: ${{ github.sha }}
