---
apiVersion: v1
kind: Service
metadata:
  name: openwebui
  namespace: maestro
  labels:
    app: openwebui
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: openwebui
