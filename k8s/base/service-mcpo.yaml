---
apiVersion: v1
kind: Service
metadata:
  name: mcpo
  namespace: maestro
  labels:
    app: mcpo
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 8002
    targetPort: 8002
    protocol: TCP
    name: http
  selector:
    app: mcpo
